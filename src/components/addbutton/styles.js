import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  icon: {
    fontSize: 30,
    marginLeft: 5,
    color: '#fff',
  },
  container: {
    flex: 1,
    flexDirection: 'row',
    marginRight: 20,
    alignItems: 'center',
  }
})
